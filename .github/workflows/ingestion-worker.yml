name: Deploying Ingestion Worker

on:
  push:
    branches: [master]
    paths:
      - "apps/ingestion-worker/**"
      - "packages/db/**"
      - "packages/qdrantdb/**"
      - "packages/common/**"
      - "docker/Dockerfile.ingestion-worker"
      - ".github/workflows/ingestion-worker.yml"
      - "scripts/deploy/deploy-ingestion-worker.sh"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.ingestion-worker
          push: true
          tags: saicharith2012/brain-cache-ingestion-worker:${{github.sha}}
          build-args: |
            DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy

      - name: Deploy to VM
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{secrets.SERVER_HOST}}
          username: ${{secrets.SERVER_USER}}
          key: ${{secrets.SERVER_SSH_KEY}}
          script: |
            IMAGE_URI=saicharith2012/brain-cache-ingestion-worker:${{github.sha}} \
            bash -s < scripts/deploy/deploy-ingestion-worker.sh
